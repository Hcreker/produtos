<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Editar Produto</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styleedit.css">
</head>

<body>
    <div class="container">
        <div class="span10 offset1">
            <div class="card">
                <div class="card-header">
                    <h3 class="well">Atualizar Produto</h3>
                </div>

                <form class="form-horizontal" action="/edit/<%= user.id %>" method="POST" enctype="multipart/form-data">
                    <div class="card-body">

                        <!-- Campo para selecionar nova imagem -->
                        <label class="control-label" for="imagem">Imagem (opcional):</label>
                        <input class="form-control" type="file" name="imagem" id="imagem"><br>

                        <!-- Demais campos -->
                        <label class="control-label" for="nome">Nome:</label>
                        <input class="form-control" type="text" name="nome" id="nome" value="<%= user.nome %>" required><br>

                        <label class="control-label" for="descricao">Descrição:</label>
                        <input class="form-control" type="text" name="descricao" id="descricao" value="<%= user.descricao %>" required><br>

                        <label class="control-label" for="fornecedor">Fornecedor:</label>
                        <input class="form-control" type="text" name="fornecedor" id="fornecedor" value="<%= user.fornecedor %>" required><br>

                        <label class="control-label" for="marca">Marca:</label>
                        <input class="form-control" type="text" name="marca" id="marca" value="<%= user.marca %>" required><br>

                        <label class="control-label" for="compra">Preço de compra:</label>
                        <input class="form-control" type="number" name="compra" id="compra" value="<%= user.compra %>" required><br>

                        <label class="control-label" for="venda">Preço de venda:</label>
                        <input class="form-control" type="number" name="venda" id="venda" value="<%= user.venda %>" required><br>

                        <label class="control-label" for="estoque">Estoque:</label>
                        <input class="form-control" type="number" name="estoque" id="estoque" value="<%= user.estoque %>" required><br>

                        <div class="form-actions">
                            <button type="submit" class="btn btn-danger">Alterar</button>
                            <a href="/" class="btn btn-primary">Voltar</a>
                        </div>

                    </div>
                </form>

            </div>
        </div>
    </div>
</body>
</html>